# Rust / Soroban
/contracts/target
/contracts/tools/input_gen/target
**/target
Cargo.lock.bak
# Circuit build output (artifacts are regenerated; the frontend copies the ACIR)
/circuits/liveness/target
/video
# Node / Next.js
node_modules
.next
out
dist
*.tsbuildinfo
.env
.env.local
.env*.local

# Stellar CLI local state & keys (never commit secrets)
.stellar
.soroban
*.secret

# Planning / internal docs (kept out of the public repo)
MIGRATION_PLAN.md
NOTES.md
/docs/internal

# OS / editor
.DS_Store
*.swp
.idea
.vscode
